# 主冷凝器 STEP 结构审查

审查对象：`主冷凝器-正样.STEP`  
审查日期：2026-08-12  
审查范围：下部两处双柱塞接口与三层冷板真空钎焊分型面的泄漏风险  
结论等级：需要结构整改；不建议仅靠增加钎料转入量产

## 1. 结论

下层与中间层之间的第一道真空钎焊面，几何上确实穿过下部两个接口的 Ø8 开口。孔、接头配合环隙、层间毛细焊缝在此形成三岔口，是明确的短泄漏路径和工艺敏感点。中间层与上层之间的第二道钎焊面不经过这两个下部接口。

首选整改方向是“下层一体接头岛 + 中层避让窝/窗”：将双柱塞接头的导入段、全部配合/密封圆柱、密封槽（若有）、限位肩和最坏公差包络全部置于下层连续母材中；第一道层间分型面在接头岛以外恢复，形成 360°闭合且具有足够有效宽度的钎焊带。

## 2. STEP 几何证据

- 文件格式：STEP AP203，SolidWorks 2025 导出，单位 mm。
- 装配结构：上层、中间层、下层，共 3 个封闭 B-Rep 实体。
- 以总装下层原点为相对坐标原点：
  - 下层/中层钎焊面：`Y′ = 6 mm`；
  - 中层/上层钎焊面：`Y′ = 14 mm`；
  - 下部两接口轴线：`X′ = ±25.5 mm, Z′ = 11 mm`，轴线平行 Y′；
  - 接口 Ø11 外圆柱段：约 `Y′ = -9.47…3 mm`；
  - Ø6.1 局部过渡柱面：约 `Y′ = 3…6 mm`；
  - 第一焊面上的开口：两处 Ø8，圆心与两接口轴线重合。
- 第一焊面两侧的拓扑完全对应：
  - 下层 `Y′=6` 主平面有两个 R4 圆边；
  - 中间层 `Y′=0` 配合面有两个同轴、同径 R4 圆边；
  - 装配后四个圆边完全重合，因此该焊面必然绕过两个接口孔口。

注意：STEP 中的 Ø6.1 是多张裁剪圆柱面形成的局部过渡特征，不能仅凭该半径把它认定为 DN4 的最终通孔或密封尺寸；最终功能尺寸应以接头规范图为准。

## 3. 失效机理

当前结构的主要风险不是一般意义上的“焊缝靠近接头”，而是三个毛细/密封体系在同一位置相交：

1. 层间钎焊间隙会与接头环隙争夺熔融钎料，导致一侧缺料、另一侧灌孔；
2. 接头局部热质量较大，峰值温度时接口区与远端板区可能存在温差，造成毛细间隙失控；
3. 三岔口容易困气，冷却后又承受钎焊残余应力、压力循环、冷热循环和外接管路弯矩；
4. 若柱塞插入/密封区延伸到第一焊面，接缝台阶、孔隙或钎料侵入都可能形成旁通或损伤密封件；
5. 单纯增加钎料可能降低局部缺料概率，但同时增加堵塞流道和基材钎蚀风险，不能消除三岔泄漏路径。

## 4. 推荐改型

### 方案 1（首选）：下层一体接头岛 + 环形避让区

- 接头全部功能包络 E 必须在下层连续母材内，尽量一次装夹加工。
- 下层向上形成接头岛；中间层仅开避让窝或窗口，不得切入密封圆柱/密封槽。
- 第一层间钎焊带绕接头岛闭合 360°，任何位置都不能被流道、螺孔、排气槽或定位缺口截断。
- 定义：
  - E：接头最大功能包络，含导入段、全部配合/密封区、限位肩、位置度/同轴度、热变形和后加工余量；
  - S_eff：E 到最近分型面的连续母材隔离桥；
  - W_eff：从压力湿边到干边的最小有效钎焊跨越宽度，不是焊环总周长。
- 仅用于首轮 CAD 空间筛选、不能直接放行生产的起始值：
  - `S_eff ≥ max(2 × t_min, 3 mm)`；
  - `W_eff ≥ max(3 × t_min, 4–5 mm)`。
- 如果两个独立圆岛之间只剩很窄的焊料颈，可改成共同的跑道形接头座；但冷热工质进出口之间仍需保留连续、足宽的分隔焊带，防止内部短路。
- 密封孔建议钎后留量精镗/铰，消除钎焊翘曲、接缝台阶或少量钎料侵入对密封面的影响。

### 方案 2：端口移到底面或侧面

在下层内部用 L 形或斜钻孔连接热工质流道，使接头座完全落在下层、远离层间焊缝。可靠性可接近方案 1，但要重新核对安装空间、死体积、压降、清洁性以及工艺堵头的压力边界和检漏可达性。

### 方案 3：单件贯穿套筒

用一个连续厚壁套筒跨过中、下层，双柱塞只在套筒连续内孔中密封；两层分别在套筒外圆的不同轴向位置形成独立 360°钎焊环，两环之间保留连续实体隔离段。该方案适用于板厚不足以形成一体接头岛的情况，但焊口更多、热质量更大、检测更困难，优先级低于前两项。

### 临时过渡方案

若近期完全不能改几何，可针对最小/名义/最大间隙和钎料量做 DOE，并配置定质量环形预成形钎料、控隙工装、排气路径及防溢料结构；也可评估两次钎焊，但第二次峰温必须低于第一次钎缝固相线并验证材料相容性。该方案仍保留三岔路径，只适合试验验证，不建议作为量产根治。

## 5. 暂不能拍板的尺寸

当前 STEP 不含基材、镀层、钎料牌号、钎焊温度曲线、MAWP、设计/瞬态温度、热工质腐蚀性、接头供应商规范、管路最大弯矩/扭矩和适用压力规范。因此以下项目必须补齐数据后计算或试验确定：

- 室温及峰温时的目标毛细间隙及公差；
- 接头岛外径、最小壁厚、圆角、S_eff 和 W_eff；
- 接头插入深度、两密封区间距、粗糙度、圆度与同轴度；
- 钎料槽宽深、预成形圈质量、毛细阻断和溢料容纳体积；
- 允许钎蚀量、设计温度下钎缝许用强度及安全系数。

同材异膨胀以外的配合应从峰温目标间隙反算，至少包含母材热膨胀、镀层/钎料厚度和最坏公差。弯矩或剥离载荷应使用 FEA/试验评估，不能只按均匀剪切计算。

## 6. 建议验证顺序

1. 装接头前对裸钎焊芯体做定量氦检；装接头后再做一次，以区分钎缝失效与柱塞/O 形圈装配失效。
2. 热工质回路充氦、冷却水回路抽空并外部嗅探；再互换回路，分别确定外漏与冷热回路串漏。
3. 制作与实件相同材料、表面状态、方向和局部热质量的“接头—层间交叉”试片，做最小/名义/最大间隙 × 钎料量 DOE，并在四象限做金相。
4. 实件接头岛和远端板区布置热电偶，验证真空钎焊峰温差、保温和排气。
5. 首件做 CT 或破坏剖切；CMM 检查密封孔圆度、同轴度、插入深度、板翘曲和钎料侵入。
6. 按适用规范进行设计温度下的液压耐压/爆破；充分真空烘干后再做氦检。
7. 压力循环、冷热循环、管路弯矩/扭矩和振动后重复定量氦检，并复核流阻、流量分配和换热性能。

验收规范应分别定义外漏与冷热回路串漏限值。水检后不要直接做高灵敏氦检，水或氧化物可能暂时封堵微毛细漏道。

## 7. 下一版图纸至少应新增的控制项

- E、S_eff、W_eff 三条边界及最坏公差状态；
- 密封孔钎前/钎后加工基准和余量；
- 钎料预成形件牌号、质量、放置方向及防错；
- 装配控隙、止口作用、排气方向及禁止钎料区；
- 真空钎焊炉温测点、清洁度和表面处理要求；
- 裸芯体与总成的独立外漏/串漏验收限值。

附图：`condenser_review_schematic.png`。
